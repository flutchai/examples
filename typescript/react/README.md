# ReAct Graph

LangGraph-based orchestrator that implements a generic ReAct loop over MCP tools. The graph handles planning, tool execution, reflection, and final response generation so it can serve any tool catalog, not just email or KB scenarios.
