# Agentic Support Graph 2025 Environment Configuration

# === REQUIRED ENVIRONMENT VARIABLES ===

# Backend API Configuration
API_URL=http://amelie-service
INTERNAL_API_TOKEN=internal-secret-token-2025

# Database Configuration
MONGO_URI=mongodb://localhost:27017/amelie-dev
# Alternative: DATABASE_URL=mongodb://localhost:27017/amelie-dev

# === OPTIONAL ENVIRONMENT VARIABLES ===

# Redis Configuration (for caching and session management)
REDIS_URL=redis://localhost:6379

# Node Environment
NODE_ENV=development

# Port Configuration
PORT=3005

# Kubernetes Service Discovery (auto-detected if running in k8s)
# KUBERNETES_SERVICE_HOST=auto-detected

# === DEVELOPMENT/PRODUCTION SPECIFIC ===

# Development Mode Settings
# - Uses ioredis-mock instead of real Redis
# - Enables verbose logging
# - Uses fallback MongoDB connection

# Production Mode Settings (when NODE_ENV=production)
# - Requires all environment variables to be set
# - Uses production-grade error handling
# - Enables performance monitoring

# === OPENAI/LLM Configuration ===
# These should be configured via Model Catalog, but can be overridden here
# OPENAI_API_KEY=your-openai-key
# ANTHROPIC_API_KEY=your-anthropic-key

# === MONITORING AND LOGGING ===
# LOG_LEVEL=debug # Options: error, warn, log, debug, verbose

# === SECURITY ===
# JWT_SECRET=your-jwt-secret # If using custom JWT validation